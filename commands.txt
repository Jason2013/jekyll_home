# Useful bash commands
echo -e "\nUSEFUL BASH COMMANDS\n"

# Check size of all contents in current directory
du -h --max-depth=1 | sort -hr

# Clone smallest repo possible
git clone --depth=1 https://repository-url
